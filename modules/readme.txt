1. Copy modules into bundles/data/osgi/modules

2. In a terminal:
telnet localhost 6666

Some commands:

bundles: list bundles and status
start [bundle id]: starts a bundle
stop [bundle id]: stops a bundle
uninstall [bundle id]: uninstalls a bundle
